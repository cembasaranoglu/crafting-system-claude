# Minimum User Input Request

Use this only when blocking gaps remain.

## Blocking inputs needed

1. `<field name>`
- Why needed:
- Downstream stage blocked:
- Accepted format:
- Example:

## Resume prompt

```text
Continue the previous stage using the existing artifacts. Here is the missing input:

<user fills only the requested fields>

Do not restart broad discovery unless this new input conflicts with prior artifacts.
```
