# Readiness Board

Static package readiness: **100/100 Strong** after local validation. Runtime marketplace/publication readiness requires manual Claude Code load validation because this environment does not include the `claude` CLI.

Manual check:

```bash
claude --plugin-dir ./plugin/crafting-system --debug
```
