---
name: runtime-drift-detector
description: Compare runtime surfaces (HTTP, CLI, jobs, events, MCP) against documented or generated descriptors. Use when checking surface drift after refactor.
---

# runtime-drift-detector

Enumerate live surfaces. Diff against descriptors. Report drift with severity.

Primary prompts:

- `prompts/102_runtime_surface_drift_detector.md`
