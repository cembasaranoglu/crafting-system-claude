# Failure Mode Catalog

Common false positives: docs-only features classified as implemented, OpenAPI paths without handlers, handlers without storage, mock-only tests, config examples not accepted by runtime parser, Kubernetes probes without endpoints, README quickstart drift, unsafe Git commits including `.env`, and readiness claims without evidence.
